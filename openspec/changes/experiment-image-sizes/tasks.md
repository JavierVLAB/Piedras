## 1. Scene.js Configuration

- [x] 1.1 Add `this.idleImageScale` to `Scene` constructor
- [x] 1.2 Add configuration comment in the "CONFIGURABLE" section of `Scene.js`

## 2. Rendering Implementation

- [x] 2.1 Update `renderIdleImage` in `Scene.js` to use `this.idleImageScale`
- [x] 2.2 Verify the image scales correctly with different values
